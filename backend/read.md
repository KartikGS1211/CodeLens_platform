You can confidently say:

“I built an asynchronous AI code-analysis pipeline using Groq LLM. The system attempts real AI evaluation first and gracefully degrades to a deterministic fallback when the model is unavailable, while persisting results to the database for consistency and performance.”

for migration put these way 
npx prisma migrate dev -n add_module_complexity